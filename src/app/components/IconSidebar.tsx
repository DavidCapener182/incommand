"use client";

interface IconSidebarProps {
  navigation?: any;
  activeItem?: any;
  onItemClick?: any;
  title?: string;
}

export default function IconSidebar(_props: IconSidebarProps) {
  // temporary placeholder – renders nothing for now
  return null;
}