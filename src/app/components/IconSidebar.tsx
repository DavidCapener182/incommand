"use client";
import React from "react";

/**
 * Placeholder sidebar component used to satisfy imports from legacy pages.
 * If you have an existing sidebar implementation you would like to display,
 * feel free to replace this component with a fully-featured version.
 */
const IconSidebar: React.FC = () => {
  return null;
};

export default IconSidebar;